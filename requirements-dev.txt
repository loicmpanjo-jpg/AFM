-r requirements.txt

# Testing (dev/CI only — not needed in the production image)
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0  # already in requirements.txt, kept here for clarity (TestClient)
